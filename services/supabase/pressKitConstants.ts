/** Limite de tamanho por arquivo enviado ao press kit (50 MB). */
export const PRESS_KIT_MAX_FILE_BYTES = 50 * 1024 * 1024;

/** Bucket Storage `press_kit`: caminho `{user_id}/press_kit/{artist_id}/arquivo`. */
export const PRESS_KIT_STORAGE_BUCKET = 'press_kit';
